export const Header = () => {
  console.log('Header');

  return (
    <header className='header'>
      This is the header
      <div> This is inside the header</div>
      Hello
    </header>
  );
};
